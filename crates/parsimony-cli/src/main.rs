//! `parsimony` CLI. Currently a placeholder — Phase 2 will wire in the
//! recipe loader and packer.
//!
//! Target subcommands (per `docs/parsimony-design.md` §14, Phase 2):
//!
//!   parsimony pack <recipe.json> -o <out.simularium> [--seed N]

fn main() {
    eprintln!("parsimony: not yet implemented — see docs/parsimony-design.md");
    std::process::exit(1);
}
